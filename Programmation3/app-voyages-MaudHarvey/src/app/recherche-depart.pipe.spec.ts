import { RechercheDepartPipe } from './recherche-depart.pipe';

describe('RechercheDepartPipe', () => {
  it('create an instance', () => {
    const pipe = new RechercheDepartPipe();
    expect(pipe).toBeTruthy();
  });
});
