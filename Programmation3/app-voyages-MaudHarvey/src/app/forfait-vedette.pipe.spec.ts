import { ForfaitVedettePipe } from './forfait-vedette.pipe';

describe('ForfaitVedettePipe', () => {
  it('create an instance', () => {
    const pipe = new ForfaitVedettePipe();
    expect(pipe).toBeTruthy();
  });
});
