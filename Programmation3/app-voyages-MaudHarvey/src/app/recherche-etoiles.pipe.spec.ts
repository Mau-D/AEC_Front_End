import { RechercheEtoilesPipe } from './recherche-etoiles.pipe';

describe('RechercheEtoilesPipe', () => {
  it('create an instance', () => {
    const pipe = new RechercheEtoilesPipe();
    expect(pipe).toBeTruthy();
  });
});
