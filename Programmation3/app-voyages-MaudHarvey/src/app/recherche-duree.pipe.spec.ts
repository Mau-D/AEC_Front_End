import { RechercheDureePipe } from './recherche-duree.pipe';

describe('RechercheDureePipe', () => {
  it('create an instance', () => {
    const pipe = new RechercheDureePipe();
    expect(pipe).toBeTruthy();
  });
});
